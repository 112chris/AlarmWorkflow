@model DateTime?
@if (Model.HasValue)
{
    @Html.TextBox("", String.Format("{0:dd.MM.yyyy}", Model.Value), new { @class = "textbox", @style = "width:400px;" })
}
else
{
    @Html.TextBox("", String.Format("{0:dd.MM.yyyy}", DateTime.Now), new { @class = "textbox", @style = "width:400px;" })
}